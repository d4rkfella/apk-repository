name: Test

on: 
  workflow_dispatch:

permissions:
  contents: read

jobs:  
  build:
    runs-on: apk-repository-runner
    container:
      image: ghcr.io/d4rkfella/melange-autoupdate:1.0.0
      volumes:
        - ${{ github.workspace }}:${{ github.workspace }}
    steps:
      - name: Check for dockerenv file
        run: melange keygen ${{ github.workspace }}/melange.rsa
