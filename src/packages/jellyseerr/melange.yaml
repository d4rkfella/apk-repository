package:
  name: jellyseerr
  version: "2.5.2"
  epoch: 0
  description: Open-source media request and discovery manager for Jellyfin, Plex, and Emby.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - nodejs-23
environment:
  environment:
    REPRODUCIBLE: false
  contents:
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    repositories:
      - https://packages.wolfi.dev/os
    packages:
      - wolfi-baselayout
      - build-base
      - busybox
      - bash
      - curl
      - yq
      - jq
      - patch
      - coreutils
pipeline:
  - uses: git-checkout
    with:
      expected-commit: "fac453878e3d55b93cfda78908403cce348bb9fa"
      repository: https://github.com/fallenbagel/jellyseerr
      tag: v${{package.version}}
      destination: jellyseerr
  - name: Install dependancies
    runs: |
      PATH="$HOME/.volta/bin:$PATH"
      curl -sSL https://get.volta.sh | bash
  - name: Build
    working-directory: /home/build/jellyseerr
    runs: |
      PATH="$HOME/.volta/bin:$PATH"
      export COMMIT_TAG=$(git rev-parse --short HEAD)
      export NEXT_TELEMETRY_DISABLED=1
      export CYPRESS_INSTALL_BINARY=0
      patch -Np1 -i "../jellyseerr.patch"
      volta install "node@$(yq -r '.engines.node' package.json)"
      volta install "pnpm@$(yq -r '.engines.pnpm' package.json)"
      sed -i 's/husky install//' package.json
      echo "{\"commitTag\": \"${COMMIT_TAG}\"}" > committag.json
      pnpm install --frozen-lockfile
      pnpm build
      pnpm prune --prod --ignore-scripts
  - name: Delete linux-musl node-modules and other unneeded files
    working-directory: /home/build/jellyseerr
    runs: |
      find node_modules -type d \
        -name "*musl*" \
        -print \
        -exec rm -rf {} + \
        -prune
  - name: Prepare package
    working-directory: /home/build/jellyseerr
    runs: |
      install -dm755 "${{targets.destdir}}/usr/lib/jellyseerr"
      cp -dr --no-preserve='ownership,timestamp' .next dist public node_modules "${{targets.destdir}}/usr/lib/jellyseerr"
      cp -d --no-preserve='ownership,timestamp' package.json jellyseerr-api.yml next.config.js "${{targets.destdir}}/usr/lib/jellyseerr"
      rm -rf "${{targets.destdir}}/usr/lib/jellyseerr/.next/cache"   
      find "${{targets.destdir}}/usr/lib/jellyseerr/.next" -type f -print0 | xargs -0 sed -i "s^/home/build/jellyseerr^/usr/lib/jellyseerr^g"
test:
  environment:
    contents:
      packages:
        - curl
        - bash
        - jq
  pipeline:
    - name: Check for missing dependancies
      uses: test/tw/ldd-check
      with:
        packages: ${{package.name}}
    - name: Run test script
      runs: |
        EXPECTED_VERSION="${{package.version}}" ./test.sh
update:
  enabled: true
  github:
    identifier: fallenbagel/jellyseerr
    strip-prefix: v
