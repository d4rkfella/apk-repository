package:
  name: postgres-init
  version: "1.0.0"
  epoch: 0
  description: "Postgres init binary"
  copyright:
    - license: MIT

environment:
  contents:
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    repositories:
      - https://packages.wolfi.dev/os
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/d4rkfella/postgres-init
      branch: main
      expected-commit: ad373211242ab8ce94da754a62aeece458e0ba9a
  - uses: go/build
    with:
      packages: postgres-init.go
      output: postgres-init
      strip: "-s -w"
