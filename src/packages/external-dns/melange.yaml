package:
  name: external-dns-custom
  version: "0.16.1"
  epoch: 3
  description: Configure external DNS servers (AWS Route53, Google CloudDNS and others) for Kubernetes Ingresses and Services.
  copyright:
    - paths:
        - '*'
      license: Apache-2.0

environment:
  contents:
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    repositories:
      - https://packages.wolfi.dev/os
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go

pipeline:
  - uses: git-checkout
    with:
      expected-commit: ece689c3bef227766b96d7b1ed53ba8b982c5c5f
      repository: https://github.com/d4rkfella/external-dns
      branch: master

  - uses: go/bump
    with:
      deps: |-
        github.com/golang-jwt/jwt/v5@v5.2.2

  - runs: |
      # Our global LDFLAGS conflict with a Makefile parameter
      unset LDFLAGS
      make build
      mkdir -p ${{targets.destdir}}/usr/bin
      mv build/external-dns ${{targets.destdir}}/usr/bin/

  - uses: strip

subpackages:
  - name: ${{package.name}}-bitnami-compat
    pipeline:
      - name: Create symlinks
        runs: |
          mkdir -p "${{targets.contextdir}}/opt/bitnami/external-dns/bin"
          ln -s /usr/bin/external-dns "${{targets.contextdir}}/opt/bitnami/external-dns/bin/external-dns"
    test:
      environment:
        contents:
          packages:
            - wolfi-baselayout
            - busybox
            - ${{package.name}}
      pipeline:
        - runs: |
            /opt/bitnami/external-dns/bin/external-dns --version
            /opt/bitnami/external-dns/bin/external-dns --help

update:
  enabled: true
  github:
    identifier: kubernetes-sigs/external-dns
    strip-prefix: v
    tag-filter: v

test:
  environment:
    contents:
      packages:
        - busybox
        - wolfi-baselayout
  pipeline:
    # AUTOGENERATED
    - runs: |
        external-dns --version
        external-dns --help
